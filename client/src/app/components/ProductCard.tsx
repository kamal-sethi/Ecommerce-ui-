"use client";

import { ProductType } from "@/types";

const ProductCard = ({ product }: { product: ProductType }) => {
  return <div className="w-full">ProductCard</div>;
  // return <div className="bg-green-200">ProductCard</div>;
};

export default ProductCard;
